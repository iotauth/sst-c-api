name: Minimal Test
on:
  push:
    branches: [ "cpp-api" ]
  pull_request:
    branches: [ "cpp-api" ]

jobs:
  test:
    name: Minimal Test
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Say hello
      run: echo "Hello from minimal CI!"
